services:
  - type: web
    name: greenroom-simulator
    runtime: node
    plan: free
    region: oregon
    buildCommand: ""
    startCommand: node server.js
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
